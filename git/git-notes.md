# Git Notes

## Pushing from Terminal
- Make sure you have a token with the appropiate permissions
- Commit changes first then push

## Guide to Pushing from terminal
- log in
- commit all changes first with "git commit -a"
- finally "git push"
